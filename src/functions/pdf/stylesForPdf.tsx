import type { StyleDictionary } from "pdfmake/interfaces";

export const wniosekStyles: StyleDictionary = {
    header: {
        fontSize: 18, // Większy rozmiar
        bold: true, // Pogrubienie
        // Możesz dodać więcej opcji, np. color, italics, letterSpacing
      },
}